# Problems in Week 10

>   TIME

[TOC]

## 1. 小明的加减法【用函数实现】

**题目描述：**

叛逆期的小明什么都喜欢反着做，连看数字也是如此（负号除外），比如：

小明会把 1234 它看成 4321；把 -1234 看成 -4321；把 230 看成 032 (032 = 32)；把 -230 看成 -032 (-032 = -32)。

现在，小明做了一些 a + b 和 a - b 的题目(a, b 为整数且不含前导 0)，如果给你这些题目正确的答案，你能猜出小明会做得到什么答案吗？

**输入格式：**

两个整数 x, y (-1000000 < = x, y < =1000000), x 表示 a + b 的正确答案，y 表示 a - b 的正确答案。

输入保证合法，且不需考虑 a 或 b 是小数的情况。

**输出格式：**

输出两个整数 s t，之间用一个空格分开，其中 s 表示小明将得到的 a + b 答案，t 表示小明将得到的 a - b 答案。 

---

**样例输入 1：**

```in
20 6
```

**样例输出 1：**

```out
38 24
```

---



## 2. 递归问题

**题目描述：**

Ackermann 函数 ack(m,n) 采用以下递归式定义：

ack(0, n) = n + 1

ack(m, 0) = ack(m - 1, 1)

ack(m,n) = ack(m - 1, ack(m, n - 1))

其中，m > 0, n > 0

编写一个计算此函数的递归函数。

**输入格式：**见样例

**输出格式：**见样例

---

**样例输入 1：**

```in
1,1
```

**样例输出 1：**

```out
3
```

---

**样例输入 2：**

```in
3,2
```

**样例输出 2：**

```out
29
```

---



## 3. 分数序列求和*

**题目描述：**

有一分数序列如下： 2 / 1, 3 / 2, 5 / 3, 8 / 5, 13 / 8, 21 / 13 … 即后一项的分母为前一项的分子，后项的分子为前一项分子与分母之和，求其前 n 项之和。 

**输入格式：**项数n（整数）

**输出格式：**前n项之和（实数，注：保留4位小数）

---

**样例输入 1：**

```in
6
```

**样例输出 1：**

```out
10.0071
```

---



## 4. 求单词的最大和最小

**题目描述：**

定义包含 5 个英文单词的字符数组（即二维字符数组），键盘输入 5 个单词，按字典顺序寻找最大和最小单词并输出。 

**输入格式：**5个单词 

**输出格式：**最大和最小单词（按字典序） 

---

**样例输入 1：**

```in
monday
tuesday
thursday
friday
january
```

**样例输出 1：**

```out
max:tuesday
min:friday
```

---

**样例输入 2：**

```in
this
is
a
book
end
```

**样例输出 2：**

```out
max:this
min:a
```

---



## 5. 编写一个求数组中最小值元素及该元素位置的函数

**题目描述：**

编写一个求数组中最小值元素及该元素位置的函数，并编写主函数调用。要求：由键盘输入10个数，并存放于整型数组中。

>   **提示：**需要定义含整型数组，最小元素值和该元素位置的结构体变量。以保证函数能同时返回最小元素值和该元素位置两个变量。

**输入格式：**用户输入的10个数

**输出格式：**第一个数字为数组中的最小元素值 第二个数字为该元素在数组中的下标位置（两个数用空格分开）

---

**样例输入 1：**

```in
11 22 33 4 5 66 7 8 9 10
```

**样例输出 1：**

```out
4 3
```

---



## 6. 编写函数实现数字逆序排列

**题目描述：**

编写函数，函数原型如下:

`void fun(int n,char res[]);`

函数功能是将整数n的各位数字逆序排列，存放到 res 字符数组中。

**输入格式：**见样例

**输出格式：**见样例

---

**样例输入 1：**

```in
1035
```

**样例输出 1：**

```out
5301
```

---

**样例输入 2：**

```in
9680200
```

**样例输出 2：**

```out
0020869
```

---

**样例输入 3：**

```in
123456
```

**样例输出 3：**

```out
654321
```

---



## 7. 编写进制转换函数

**题目描述：**

编写进制转换函数，函数原型如下：

`void dtoboh(int n,int t,char res[]);`

功能是将十进制整数 n(n>=0) 转换为 t 进制数(即2、8及16进制)，结果存放在 res 字符数组中，并编写测试用的主函数。

**输入格式：**见样例

**输出格式：**见样例

---

**样例输入 1：**

```in
248,2
```

**样例输出 1：**

```out
11111000
```

---

**样例输入 2：**

```in
248,8
```

**样例输出 2：**

```out
370
```

---

**样例输入 3：**

```in
248,16
```

**样例输出 3：**

```out
F8
```

---

**样例输入 4：**

```in
2018,2
```

**样例输出 4：**

```out
11111100010
```

---

